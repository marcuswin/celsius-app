import { StyleSheet } from 'react-native';

const OnBoardingCurrencyInterestRateInfoTableStyle = StyleSheet.create({
  wrapper: {
    borderColor: 'rgba(200,200,200,0.3)',
    borderTopWidth: 1,
  }
});

export default OnBoardingCurrencyInterestRateInfoTableStyle;
